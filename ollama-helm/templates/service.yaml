apiVersion: v1
kind: Service
metadata:
  name: {{ include "ollama-helm.fullname" . }}
  labels:
    {{- include "ollama-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ollama-helm.selectorLabels" . | nindent 4 }}
